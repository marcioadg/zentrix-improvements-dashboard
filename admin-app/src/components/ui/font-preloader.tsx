
import { useEffect } from 'react';

export const FontPreloader = () => {
  // Inter font is now loaded directly in index.html for better performance
  // This component is kept for potential future font optimizations
  return null;
};
